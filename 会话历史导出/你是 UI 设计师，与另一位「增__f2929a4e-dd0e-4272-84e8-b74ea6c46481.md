# 你是 UI 设计师，与另一位「增

- 会话 ID: `f2929a4e-dd0e-4272-84e8-b74ea6c46481`
- 类型: 子代理会话
- 创建时间: 2026/8/25 13:53:58
- 父会话: `session-7dfccb75-6426-4d35-b5ba-a01c1825cfd4`
- Preset: cordis
- 工作目录: D:\ResuAlign-Lite

---

### 🙋 用户

你是 UI 设计师，与另一位「增长产品经理」专家并行协作，共同产出「UI 模块联动改造」的讨论成果，你的职责是把模块联动的改造落到视觉/组件设计上，并支撑新的交互节奏与引导方式。产出交付物写入 D:\ResuAlign-Lite\.scratch\expert-team-20260825\02-UID-UI模块联动改造方案.md（目录不存在就创建）。

## 共同上下文
- 仓库：D:\ResuAlign-Lite（ResuAlign 求职工作台，本地单机 Web 应用，FastAPI+原生 JS 前端）。实例运行在 http://127.0.0.1:8000（个人模式；只读访问，禁止删除/重置数据，禁止触发高耗 LLM 任务）。
- 必读：① 产品体验报告/persona-walkthrough-体验报告-20260825.md（走查报告，含视觉/UI 维度问题 P1-1/P1-4/P1-5/P2-1/P2-4/P2-5/P2-6 与汇总表）；② 已有视觉设计文档 .scratch/phase-17/design/01-visual-redesign.md、02-visual-language.md、03-brand-system.md，.scratch/phase-18/design/01-card-component-spec.md、02-motion-architecture.md、03-animation-language.md，docs/plan-2.0.1-redesign.md；③ 实际样式与组件：src/resualign/static/styles.css（约260KB，先查 :root 设计变量/品牌 token 是否已存在再复用）、src/resualign/static/app/format.js（组件渲染，如 exportDock/workbenchGuideSteps）、index.html。
- 协作约束：你与增长 PM **并行工作不能实时对话**，双方以走查报告结论为共同设计基线：全站统一动词「开始对齐」（废除「开始优化」）、失败态工作台收敛为单入口、设置词表为岗位库唯一状态数据源、空态引导文案与实际控件一致。你不得修改源码（只有「高级开发者」改代码）。

## 你的任务
产出「UI 模块联动改造」视觉/组件设计，覆盖：
1. **模块联动一致的视觉表达**（对应报告 B 维度 P1-2）：岗位库卡片状态下拉与筛选下拉同源的呈现规范；自定义词表生效后的界面反馈（设置页保存后提示「岗位库已同步」的视觉形态）；跨模块状态词统一（卡片/筛选/驾驶舱/今日待办同词同义）。
2. **工作台失败态收敛设计**（P0-2）：失败横幅组件规范（位置、等级色、关闭按钮最小尺寸、错误文案区、阶段耗时展示）、「重新运行对齐」单主按钮在顶栏的视觉层级、失败态下隐藏首次引导卡的空态替换方案；统一按钮层级（主/次/危险）。
3. **简历档案页“隐藏编辑器”回归**（P1-1）：内联编辑器默认隐藏的正确实现（CSS 层建议，如 .hidden 优先级或 [hidden] 属性），编辑态/预览态切换的视觉状态。
4. **移动端工作台重组**（P1-4，390px）：把「岗位分析/面试记录/优化设置」tabs 与主按钮放到视口可达位置的折叠面板/手风琴设计；触控目标 ≥40px；关闭按钮 ≥32px。
5. **交互节奏的视觉表达**（支撑增长 PM 的引导策略）：初级引导（空态步骤条）、渐进披露（高级选项收进「更多」）、多入口收敛后的单动作条（诊断/导出收进 ▾ 菜单）、技能缺口长句截断的视觉规则（P1-5）。
6. **情绪与信息密度**（P2-5 简历列表卡片改摘录、P2-6 设置页节奏：区块折叠、保存点收敛、Token 显隐）。

## 产出文档结构（02-UID-…md）
① 设计基线声明（采纳的统一动词、状态唯一源等，并引用走查报告与现有 token）；② 组件规范：失效/空/进行中/成功四态组件矩阵（横幅、badge、按钮层级、步骤条、卡片——引用现有 .btn--*/badge 类名或 :root 变量，不新造孤立风格）；③ 每页改造的 before→after 视觉与布局说明（工作台失败态、岗位库卡片、简历档案页、移动端工作台、设置页、驾驶舱快速继续卡）；④ 可复用组件清单与实现建议（类名/样式覆盖点，标注现有 styles.css 行号或格式）；⑤ 「给增长产品经理的对接清单」：哪些节点需要她的引导文案与触发规则才能定稿视觉，+ 3-5 个需要在下一轮合议中确认的开放问题；⑥ 文末写「待增长产品经理回应事项」。

## 返回给主会话
文档路径+一页摘要：核心设计决策、你给增长 PM 的 3-5 个开放问题、你认为视觉上最应先落地的高杠杆改造。

### 🤖 助手

Now let me read the existing design documents in parallel.

### 🤖 助手

Let me quickly verify the today page status words and the settings section headers to make the cross-module word table accurate.

### 🤖 助手

I now have complete context. Let me create the deliverable document.

### 🤖 助手

交付完成。文档已写入 **`D:\ResuAlign-Lite\.scratch\expert-team-20260825\02-UID-UI模块联动改造方案.md`**（目录已自动创建）。

---

## 一页摘要

### 核心设计决策（与增长 PM 的共同基线，B-1~B-5）
1. **全站统一动词「开始对齐」**——按走查报告 P0-2 采纳；代码证据 `format.js:2141` 仍输出「开始优化」，空态步骤文案三处与实况 UI 不符，一并对齐（B-1/B-4）。
2. **状态唯一源 = 设置词表**——卡片状态下拉（`format.js:941/985` 硬编码 `JOB_STATUS_CANONICAL`）改为与筛选、编辑弹窗同源 `vocabularyList("statuses")`；状态含 lifecycle 硬语义，约束「仅改名、不增删」；保存词表后视觉反馈「岗位库已同步」（`badge-teal` + toast），替代现 `main.js:2966` 的裸 toast（B-2）。
3. **失败态单入口收敛**——顶栏唯一 `btn-danger-solid`「重新运行对齐」；横幅 v2 规范：4px 红左条 + 原因/下一步 + 阶段耗时（tabular-nums）+ ≥32px 关闭钮，**横幅内不再放第二颗按钮**；失败态不渲染首次引导卡，改为「上次运行摘要」inset 卡（B-3）。
4. **简历隐藏编辑器**双保险修复：初始渲染挂 `hidden` 属性（全局 `[hidden]{display:none!important}` 在 `styles.css:9-11` 已存在）+ 在 `:9568` 后追加 `.resume-inline-edit.hidden{display:none!important}` 防回退。
5. **移动端重组**：390px 下 tabs 保持 sticky + 三个面板改手风琴 + 底部固定动作条（主按钮全宽、safe-area 内边距）；触控 ≥40px、关闭 ≥32px。
6. 另覆盖：驾驶舱快速继续卡失败标识（红左条 + 「上次失败 · 重新运行」）、技能缺口截断规则（>12 字/含「或」「等」不进缺口，前端 24ch 单行 + title）、简历列表摘录卡、设置页折叠/保存点收敛/Token 显隐、跨模块状态词统一表（含 P1-6 待跟进口径）。

### 给增长 PM 的开放问题（需合议拍板）
- **Q1**：状态词表「仅允许改名、不增删」是否接受？增删会破坏五列 lifecycle 顺序语义（视觉默认按约束设计）。
- **Q2**：「开始对齐」动词覆盖边界——是否含「AI 优化（模块化润色）」、驾驶舱「继续对齐」（建议→「继续」）？
- **Q3**：失败横幅可关闭后，失败详情的二次入口放哪（顶栏按钮保留，详情入口需定）？
- **Q4**：移动端 bottom 固定动作条 vs 顶部 sticky 动作行——底栏占 64px 视口的取舍。
- **Q5**：简历卡片摘录字段来源（正文前两段 vs 诊断结论摘要），需要谁提供字段。

### 视觉上最应先落地的高杠杆改造
**③-1 工作台失败态收敛（P0-2）**——复用既有 token 与 `btn-danger-solid`/`.align-error-banner` 两个现成壳，纯收敛无需新架构，走查估 0.5-1 天，直接消除「3 入口 2 标签 + 误导归因」这个核心信任崩塌点，且它为 ③-6 驾驶舱失败卡提供了同一套失败视觉语言。次优先是 ③-3 简历页隐藏编辑器（CSS/标记一行修复，0.5 天，信任影响大但改动极小）。

> 协作状态：视觉侧待增长 PM 的 ⑥-① 空态文案与 ⑥-② 失败分类文案后即可定稿；文档内含完整样式覆盖点行号，可直接交给「高级开发者」落地。
