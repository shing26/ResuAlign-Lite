# 对 ResuAlign-Lite 项目（个人自托

- 会话 ID: `66d6a177-b9c4-4df9-83c9-91e96f5c958d`
- 类型: 子代理会话
- 创建时间: 2026/8/25 02:59:42
- 父会话: `session-24d59544-0f41-4002-abb6-2df03a5f24c4`
- Preset: standard
- 工作目录: D:\ResuAlign-Lite

---

### 🙋 用户

对 ResuAlign-Lite 项目（个人自托管求职工作台）做一次完整的用户体验走查（目标用户画像逐屏走查），并产出体验报告。你作为「用户体验设计师」，只做分析、只写报告文件，**绝对不要修改任何 `src/` 代码**。

## 项目背景（已为你核实）

- 仓库：`D:\ResuAlign-Lite`。产品定位：个人求职者自托管求职工作台，闭环 = 筛岗 → 定制 → 投递 → 跟进。核心是 LLM 驱动的简历-岗位对齐流水线，UI 中文。
- 技术栈：FastAPI 后端 + **no-build 原生 JS 前端**（`src/resualign/static/index.html` + `styles.css` + `app.js` 及 `src/resualign/static/app/` 下 13 个模块），无打包器、无框架、无图标库。
- 四大工作台模块：简历中心（主简历版本/回滚/独立诊断）、岗位库（粘贴/链接/批量导入/爬取/分类/待分类降级）、工作台（单岗位对齐：开始优化→进度→Diff 采纳→定稿→导出 PDF/Markdown/JSON）、设置（评估权重/薪资参照/分类词表/成本护栏）。另有驾驶舱（dashboard）、投递闭环（记录投递→投递快照抽屉→安排跟进→提醒）、命令面板（Ctrl+K）、看板（kanban）。
- 数据全部本地 SQLite，默认个人模式（无登录墙）。
- 刚完成 ADR-0033「消费者视觉刷新」：默认浅色、A4 简历纸隐喻、emoji→SVG 线性图标、术语统一（「投递快照」唯一叫法）、卡片式设计+组件动效（prefers-reduced-motion 门控）、明暗 toggle。相关设计文档：`docs/adr/0033-consumer-visual-refresh.md`、`.scratch/phase-17/design/`、`.scratch/phase-18/design/`、根目录 `ux-report.md`（注意该文件是 GBK 乱码，读 `产品体验报告/` 目录下的 UTF-8 报告即可）。
- 已有 QA 基准（供你对齐已知问题，不要重复劳动）：`产品体验报告/` 下 Round2（2026-08-19）与 Round4（2026-08-21）报告已覆盖：P0 对齐默认参数必败（已在 ba44919 修复）、今日待办与驾驶舱计数矛盾、跟进保存假成功、导出去结构化、移动端导航被推出屏等。你的报告应**验证这些是否已修复 + 找出新问题**，并明确区分「已修复」「仍存在」「新发现」。

## 走查方式（任选，建议组合）

1. **真实浏览器走查（推荐）**：用隔离实例，参考现成脚本 `.scratch/phase-20/playwright_smoke.py` 或 `.scratch/phase-16/playwright_smoke.py`（独立端口 + 独立数据目录，避免触碰真实 8000 服务与 `data/jobs.db`）。桌面 1440×900 + 移动 390×844 都要走查。LLM 调用可用 `.scratch/phase-20/fake_llm.py` 或真 LLM（`.env` 已配 deepseek-v4-flash，注意额度）。
2. **代码/静态走查**：读 `src/resualign/static/app/*.js`（main.js 约 125KB、format.js 约 178KB、split-canvas.js、events.js、dashboard-view.js 等）与 `styles.css`，检查模板字符串、事件流、data-action 契约、aria/data 属性、打印样式。
3. 走查前可先 `git log --oneline -15` 了解最近改动范围。

## 必须逐屏走查的核心旅程（目标画像：新用户 + 深度创作者）

- 冷启动：首屏驾驶舱 → 空状态是否引导清晰
- 简历中心：上传/新建主简历 → 独立诊断 → 版本历史/回滚
- 岗位库：添加岗位（粘贴/链接/批量导入）→ 分类（含失败降级「分类待定」→重新分类）→ 列表筛选/编辑
- 工作台（重点）：选岗位+选主简历 → 开始优化 → 进度反馈 → Diff 采纳/拒绝 → 定稿 → 刷新恢复 → 导出 → 另存为新主简历
- 投递闭环：记录投递 → 投递快照抽屉 → 安排跟进（面试阶段/到期时间）→ 今日待办/提醒 → 状态迁移（未投递→已投递→面试中→offer/拒绝）→ 看板
- 设置：权重/词表/薪资参照/成本护栏 → 保存立即生效 → 与岗位库下拉联动
- 命令面板 Ctrl+K、明暗 toggle、移动端导航

## 报告要求（写入仓库，中文，文件名建议 `.scratch/ux-walkthrough-{日期}.md` 或 `产品体验报告/Round5-体验走查-{日期}.md`）

按以下结构输出：
1. **体验总览**：健康指数（🔴阻断/🟠高/🟡中/🟢低）、一句话核心体感
2. **分严重级别的缺陷清单**：每项给 [严重级别] [编号]，含复现步骤、预期 vs 实际、根因（代码位置/文件）、修复建议（具体到可落地）
3. **交互设计逻辑不合理专项**：流程断点、误导文案、状态不一致、操作不可达、确认/取消语义混乱
4. **功能模块联合度专项**：跨模块数据/状态不同步、入口缺失/重复、耦合不足造成体验割裂（如驾驶舱 vs 今日待办 vs 看板计数一致性、岗位库 vs 工作台 vs 设置词表联动）
5. **UI 界面设计不足专项**：视觉层级、可扫读性、反馈可用性、移动端 390px 溢出/触控目标、暗色模式、A4 打印、a11y（键盘可达/focus/aria）
6. **亮点清单**：值得保留的设计
7. **验证方法**：哪些是浏览器实测、哪些是代码走查、证据文件（截图路径等）

报告要具体、可落地：给出选择器/代码位置/截图证据，不要泛泛而谈。完成后确认文件已写入磁盘（Get-Item），并在最终回复中给出报告路径 + 摘要（健康指数、问题数、Top 3 严重问题）。
