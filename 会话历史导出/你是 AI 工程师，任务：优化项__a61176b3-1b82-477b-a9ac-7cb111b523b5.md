# 你是 AI 工程师，任务：优化项

- 会话 ID: `a61176b3-1b82-477b-a9ac-7cb111b523b5`
- 类型: 子代理会话
- 创建时间: 2026/8/25 13:53:58
- 父会话: `session-7dfccb75-6426-4d35-b5ba-a01c1825cfd4`
- Preset: cordis
- 工作目录: D:\ResuAlign-Lite

---

### 🙋 用户

你是 AI 工程师，任务：优化项目现有的 AI 模型调用层。产出交付物写入 D:\ResuAlign-Lite\.scratch\expert-team-20260825\03-AIE-AI模型调用优化方案.md（目录不存在就创建）。**你不得修改任何源码**（并行团队中只有「高级开发者」改代码），你的产出是方案文档（含可直接落地的代码级 patch 片段）。

## 项目上下文
- 仓库：D:\ResuAlign-Lite。实例运行在 http://127.0.0.1:8000（个人模式，只读访问；禁止触发高耗 LLM 任务——对齐/诊断会烧额度且当前必失败）。
- 必读：产品体验报告/persona-walkthrough-体验报告-20260825.md 的 P0-1（对齐主路径实况 166s 超时失败，错误文案误导「检查 API Key 与网络」）+ 第 7 节边界；背景：当前 LLM=openrouter meta/muse-glimmer-30b，连通性正常（设置页显示 2437ms），失败阶段 jd_analysis、elapsed=166.2s。

## 调用层代码地图（已定位，请逐一阅读核实）
- src/resualign/llm.py：OpenAIClient 核心（DEFAULT_TIMEOUT、connect 30s、per-request read timeout 注释「transport 超时不重试」、_raise_network_timeout、chat_json/_structured_or_json 结构化调用与重试次数——重点看结构化重试是否多次烧额度）。
- src/resualign/role_router.py：角色化超时分层（_role_timeout 默认表 + RESUALIGN_ROLE_TIMEOUT_<ROLE> 覆盖）+ primary/fallback 双节点（fallback_config、call_with_role）。
- src/resualign/graph/router.py：流水线节点超时表（profiler 15s / gap 15s / editor 40s max_retries 1 / eval 30s）与 graph/executor.py、graph/state.py、graph/gates.py 的执行与归约。
- src/resualign/engine.py：诊断/画像/改写/评估各阶段 client 装配（timeout=_role_timeout(...)、tailor 40s max_retries=1）。
- src/resualign/jd_analysis.py + jd_profiler.py + gap_analyzer.py：jd_analysis 阶段把 profiler+gap 两份提示词拼接成一次调用（注意与 15s+15s 独立调用混用的矛盾/超时风险）。
- src/resualign/api/services/workbench.py:681（90s client）、api/routers/jobs.py:123/562/828（30s/60s/45s）、api/services/resume_optimize.py（逐条 polish 隔离超时）。
- src/resualign/api/services/cost_guard.py、llm_usage.py：成本护栏与用量记账。
- src/resualign/context_budget.py：token 预算（与提示词工程师的产出相关，你只需给约束）。
- src/resualign/api/routers/nodes.py + settings.py：备用节点/连通性探测已存在（settings 里已有备用节点配置，但前端未接「换用备用节点重试」）。

## 你要回答的问题（写进方案）
1. **P0-1 调用侧根因**：为什么实测 166s 才失败（哪个超时起了作用？哪次调用？）？超时护栏（40s/次）与实际失败时间（166s）的差距从哪来（多阶段累加？重试？拼接后的阶段调用实际超时不同）？给出证据链（代码路径）。
2. **失败分类**：超时/结构解析失败/空内容/额度(402)/限速(rpm) 分别如何被捕获与映射（llm.py 异常类型→jobs.py 失败 code）——指出现有缺口。
3. **优化方案**（分 P0/P1/P2，每项给文件:行 + patch 片段 + 理由 + 影响面）：超时分层与护栏对齐、transport 超时后是否值得 1 次重试（短输入）、结构化输出的重试与降级（解析失败重试次数上限、fallback 到非结构化+正则）、jd_analysis 拼接调用拆回独立调用的利弊（成本/延迟/超时概率）、备用节点一键的调用层接入点、成本护栏联动（失败与重试是否计入成本上限）、并发/队列调度（scheduler.py 单 Worker）相关约束。
4. **验证方法**：如何在不烧额度前提下验证（mock/离线、用小输入 1 次/curl 读节点状态如 /api/nodes、/api/settings/status）。
5. **跨专家交接**：与「提示词工程师」（并行，管提示词内容/输出长度）的分工边界；若你的方案需要提示词侧配合（如缩短输出、拆调用），列在「交接事项」；与「高级开发者」（并行，管 P0-1 前后端实现）的交接口径。

## 返回给主会话
文档路径+一页摘要：调用侧根因结论、P0 级优化项清单（各一行）、与提示词工程师的交接要点。

### 🤖 助手

收到任务。我是 AI 工程师子代理，负责产出「AI 模型调用层优化方案」文档（不改源码）。先建任务清单，然后逐一核实代码地图。

### 🤖 助手

体验报告已读，P0-1 关键事实：`jd_analysis` 阶段 elapsed=166.2s 失败，文案误归因「检查 API Key 与网络」，设置页显示 40s/次护栏。现在逐一核实调用层代码。

### 🤖 助手

llm.py 与 role_router.py 已核实。关键发现：`DEFAULT_TIMEOUT=120s` 但实际由角色超时注入；transport 超时不重试、直接抛 `LLMResponseError`；`call_with_role` 会 primary→fallback 各一次（各吃一个角色超时）。继续读 engine.py 与 graph 层。

### 🤖 助手

engine.py 与 graph/router.py 已核实。关键点：`_llm_runner` 在 `node_store.get_active_node()` 存在时走 `call_with_role`（用 role_router 的 30s/30s/90s/60s 超时 + primary→fallback 各一次），否则用 NODE_ROUTER 节点 timeout（15s/15s/40s/30s）。继续读 graph 执行器、状态与 gates，以及 workbench 服务。

### 🤖 助手

Graph 层已核实：executor 节点失败直接抛给外层（`fail_action=mark_failed`），超时由 `llm_runner` 内的 client 决定。现在读 jd 三件套（拼接调用）与 API 层。

### 🤖 助手

workbench.py:681 已核实（90s 兜底 client）。现在读 jobs.py 关键行位（123/562/828）定位对齐任务执行路径。

### 🤖 助手

现在定位对齐任务执行路径（analysis-status / align）与 scheduler。搜索关键符号。

### 🤖 助手

scheduler.py 只是提醒器。真正运行对齐任务的是 registry/worker。搜索执行入口。

### 🤖 助手

找到 `_job_failure_detail` 的错误映射函数。

### 🤖 助手

错误映射已核实（jobs.py:32-63，timeout→「检查 API Key 与网络」文案）。现在看 JobRegistry 的 fail/elapsed 计算、对齐启动端点，以及谁用 `profile_and_gaps` 拼接调用。

### 🤖 助手

elapsed 是墙钟（含排队）。还需确认工作台「开始对齐」实际走的端点与 payload，以及 resume_optimize.py / cost_guard / llm_usage / nodes 路由。

### 🤖 助手

对齐端点是 `POST /api/jobs/{id}/workbench`（jobs.py:417）→ `_queue_job` → `_run_job` → `engine.run`（带 node_store，走 role router 30s/30s/90s/60s）。`profile_and_gaps` 拼接函数无生产调用方。继续读剩余文件。

### 🤖 助手

成本账已核实（`_observe_llm_call` finally 中每次 client 调用记一账，失败也计；primary+fallback=2 次）。继续读 context_budget、llm_nodes、nodes/settings 路由。

### 🤖 助手

llm_nodes 已核实。关键发现：`resolve_node_for_role` 无绑定时返回**当前激活节点**，而 `call_with_role` 的 fallback 也是 `get_active_node`——单节点情况下 primary 与 fallback 是**同一个节点**，超时会×2 空烧。继续读 nodes/settings 路由与 resume_optimize 的 polish_timeout。

### 🤖 助手

config 已核实。最后快速核实 ops metrics 端点与现有测试（验证方法章节要用）。

### 🤖 助手

test_llm_timeout.py 已核实——设计意图「transport 不重试、调用方(role-router fallback)决定重试」与 `call_with_role` 的 fallback 语义直接冲突（单节点时 fallback=同一节点，超时×2）。快速看 test_llm.py 与 observability 后即可成稿。

### 🤖 助手

证据链已完整。快速核实前端「40s/次」显示来源后成稿。
