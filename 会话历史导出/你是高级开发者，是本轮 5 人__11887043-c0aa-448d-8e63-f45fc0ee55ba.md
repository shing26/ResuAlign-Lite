# 你是高级开发者，是本轮 5 人

- 会话 ID: `11887043-c0aa-448d-8e63-f45fc0ee55ba`
- 类型: 子代理会话
- 创建时间: 2026/8/25 14:01:58
- 父会话: `session-7dfccb75-6426-4d35-b5ba-a01c1825cfd4`
- Preset: cordis
- 工作目录: D:\ResuAlign-Lite

---

### 🙋 用户

你是高级开发者，是本轮 5 人并行团队中**唯一允许修改仓库源码**的专家。任务：先解决走查报告「最值得先修」的三个问题——P0-1（对齐失败体验闭环）、P0-2（工作台失败态收敛）、P1-1（简历档案页隐藏编辑器默认可见）。实施后写实施报告到 D:\ResuAlign-Lite\.scratch\expert-team-20260825\05-SD-实施报告.md（目录不存在就创建）。

## 必读
- 产品体验报告/persona-walkthrough-体验报告-20260825.md：P0-1（§2）、P0-2（§2）、P1-1（§2）、§5 改进路线表、理论证据行号。
- 相关源码：src/resualign/static/app/split-canvas.js（错误横幅渲染 1283-1297、空态渲染 194-198）、src/resualign/static/app/format.js（空态引导文案 2126-2134、exportDock 等）、src/resualign/static/styles.css（:7998 .hidden、:9560-9583 .resume-inline-edit、:6308-6334）、src/resualign/api/services/jobs.py（:45-49 timeout 文案映射）、src/resualign/resume_optimize.py（:298-299 同类文案）。

## 实施范围（只动以下文件，不要碰无关模块）
### P0-1 对齐失败体验闭环
- 后端文案映射（api/services/jobs.py:45-49 与 resume_optimize.py:298-299）：把「模型响应超时，请检查 API Key 与网络连接后重试」改为按失败分类的可操作文案：超时→「模型响应超时（本次耗时 X 秒），可尝试更换更快的模型或稍后重试」，结构解析失败→「模型返回内容格式异常，请重试或更换模型」，空内容→「模型返回为空，请重试」；保留失败 code 字段供前端分类。先读这两处函数的上下文再改，保持返回结构兼容。
- 前端（split-canvas.js:1283-1297 附近）：错误横幅根据失败 code/文案做分类展示；若后端返回含 elapsed（阶段耗时）则展示；**横幅内去掉第二个「重新运行对齐」按钮**（收敛到顶栏一个入口——见 P0-2）。
- **服务注意**：运行中的 8000 服务由 start.ps1 启动（uvicorn --workers 1，**无 --reload**）：前端静态文件改完刷新即生效；后端 .py 改动需重启才生效。**不要 kill/重启 pid 15104 的现有服务**（用户可能正在用）。改为：后端改动完成后，在实施报告「交接事项」注明「需重启服务生效」。
### P0-2 工作台失败态收敛（前端）
- split-canvas.js 空态渲染分支：任务 failed/canceled/expired 时**不再渲染「还没有对齐结果」的首次引导卡**；顶栏保留危险级「重新运行对齐」单入口。
- format.js:2126-2134 空态引导文案按实际 UI 修正（按钮名统一「开始对齐」；位置描述与实际一致；全站把「开始优化」类措辞统一为「开始对齐」——先在文案层统一，涉及状态机命名不动）。
### P1-1 简历页隐藏编辑器可见（CSS）
- 修复 styles.css .hidden 与 .resume-inline-edit display:flex 的优先级冲突（:7998 vs :9560-9583），确保初始状态编辑器隐藏；顺带检查编辑态「返回当前版本/取消」可见性（:6308-6334 相关）。

## 验证要求
- node --check 所有改动的 JS 文件；对后端改动跑相关 pytest（python -m pytest tests/ -k 相关关键字，PYTHONPATH=src）；CSS 无法单测则用 DOM 探针或说明验证方式。
- 不反复跑真实 LLM 对齐（烧额度且当前必失败）；失败分类映射可用源码级检查 + 构造样例验证映射函数输出。
- 提交：git add 相关文件并 commit（仓库已有 git；commit message 注明这是走查修复：如 fix(ux): align-failure copy & workbench failed-state, resume editor hidden）。若你决定不 commit 也说明原因。

## 实施报告（05-SD-…md）结构
① 改动文件清单与 diff 摘要（每个文件：改了什么、为什么）；② 验证结果（node --check/pytest/DOM 探针实际输出）；③ 未竟事项与交接（需要重启服务、尚未覆盖的 P1-4 移动端等、给团队其他专家的交接口径：如提示词工程师的替换清单是否可下一步实施）。

## 返回给主会话
改动摘要（文件+关键 diff 行）、验证结果、交接事项（如需要重启服务）。

### 🤖 助手

I'm the SD (senior developer) on this round. Let me set up my task list and read all the required context first.

### 🤖 助手

Report read. Now let me read the source files at the cited locations.

### 🤖 助手

Now let me find `workbenchGuideHtml`, `workbenchPrimaryButtonHtml`, and read format.js + styles.css + backend files.

### 🤖 助手

Let me find the empty-state card "还没有对齐结果" and related functions.

### 🤖 助手

Found the empty-state card at `format.js:776-784` (report cites 2126-2134, which is the guide steps — I'll fix the actual empty-state copy at the real location). Now the backend files.

### 🤖 助手

Now let me see how `_job_failure_detail` is consumed and what the `analysis-status` endpoint returns (to know how to add a `code` field).

### 🤖 助手

Let me examine the job registry snapshot structure so I can add `code`/`elapsed` fields compatibly.

### 🤖 助手

Let me check the `LLMResponseError` class and whether exceptions carry a machine-readable `code`.

### 🤖 助手

Let me read the worker function in `api/services/jobs.py` around the try/except to see what timing context is available at the fail call site.

### 🤖 助手

Let me find callers of `module_failure_detail` and the resilience/retry text, and check `_registry.fail` call sites across the repo.

### 🤖 助手

Let me check every surface that displays alignment/module `error` strings, so a code prefix strategy won't leak raw tokens into the UI.

### 🤖 助手

Now let me read the three CSS regions and check what toggles the resume inline editor.

### 🤖 助手

Let me find how the resume editor is rendered/toggled in JS and HTML templates.

### 🤖 助手

Let me check the rest of resume-center.js for any post-render hidden-attribute setup, and search for other `.hidden`-toggling patterns that would be affected by a CSS change.

### 🤖 助手

Let me check how the `.hidden` class is used at runtime (removed/added) and whether playwright/puppeteer is available for a DOM probe.

### 🤖 助手

Let me check the `resume-edit` form handler (to ensure closing after save) and any display rules that could override `[hidden]` globally.

### 🤖 助手

There's already a global `[hidden]` rule at styles.css:9. Let me read it.

### 🤖 助手

Key discovery: `styles.css:9` already has a global `[hidden] { display: none !important; }` — so the **attribute**-driven hide wins over `display:flex`, while the **class** `hidden` (used in resume-center.js:183) is the one defeated. The correct P1-1 fix is to switch the markup to the attribute (1 line) + make the flex rule `:not([hidden])` defensive. Let me confirm a few remaining frontend details.

### 🤖 助手

Let me check format.js header (module importability for node tests), existing tests for the failure mapping, and badge classes.

### 🤖 助手

format.js is Node-importable pure code (unit tests exist under tests/frontend) — perfect for verifying the classification mapper. Let me read the existing backend tests that touch these functions.

### 🤖 助手

Now let me check the frontend unit-test setup under tests/frontend so I can add regression tests for the new helpers.

### 🤖 助手

Setup is clear — `node:test` with a jsdom happy-setup, plus a source-contract test file. Now let me read split-canvas.js's import block before editing.

### 🤖 助手

All context gathered. Now implementing. Starting with the backend failure mapping — `_job_failure_detail` in jobs.py:

### 🤖 助手

Now the except handler in the same file — compute elapsed (guarded) and pass it:
