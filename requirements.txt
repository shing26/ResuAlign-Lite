# Production dependencies - flat copy generated from [project.dependencies] in
# pyproject.toml (single source of truth). Dev/test deps live in the `dev`
# extra there; for CI/tests use: pip install -e ".[dev]"
# Keep this file in sync with pyproject.toml when adding production deps.
httpx>=0.27.0
PyMuPDF>=1.23.0
python-docx>=1.1.0
pydantic-settings>=2.0.0
fastapi>=0.115.0
python-multipart>=0.0.9
uvicorn[standard]>=0.32.0
beautifulsoup4>=4.12.0
colorama>=0.4.6
mcp>=1.2.0
