# R5 验收报告 — DOM 走查（2026-08-25）

- **验收对象**：ResuAlign 求职工作台，运行实例 `http://127.0.0.1:8000`（个人模式，单进程 uvicorn，静态文件实时读盘、无 Cache-Control，刷新即生效）
- **验收变更**：R5 三 commit
  - `bc15036` — P1-2 岗位卡状态下拉与筛选/编辑同源（设置词表）
  - `f33b79a` — P1-3 驾驶舱快速继续失败标识
  - `8e58140` — P1-4 移动端工作台可达性最小集
- **方法**：Playwright（Chromium headless，`C:\Users\Shing\AppData\Local\ms-playwright\chromium-1228`）双视口实机走查：
  - 移动 390×844（`isMobile + hasTouch`）
  - 桌面 1440×900
  - 全程捕获 console error / pageerror / requestfailed，零错误
  - 只读走查：未触发任何 LLM 调用、未写任何数据、未改源码、未重启服务

---

## ① 走查方法与覆盖清单

| 维度 | 覆盖 |
|---|---|
| 路由 | `#/jobs`、`#/dashboard`、`#/workspace/{job_id}`、`#/workspace`（空态）、`#/resumes` |
| 视口 | 390×844（P1-4 主测）、1440×900（P1-2 主测 + 回归） |
| API 只读探针 | `GET /api/settings`（词表）、`GET /api/dashboard`、`GET /api/jobs`、`GET /api/jobs/{id}`（alignment_status）、`GET /static/...`（5 个静态文件 marker 检查） |
| 数据状态 | 11 个岗位：`alignment_status` = succeeded ×5、idle ×6；**无 failed/canceled/expired**（jobs.db `library_jobs` 全表 + 全库扫表确认，6f0378d4 早前失败记录已回 idle） |
| 单测佐证 | `node --test` 运行 R5 相关 3 个测试文件：**45/45 通过**（含 P1-2 词表 4 例、P1-3 变体 4 组、P1-4 底栏 3 例） |
| 截图 | `D:\ResuAlign-Lite\产品体验报告\ui-shots\r5-*.png`（10 张，见逐项表） |

**前置检查（静态文件已含 R5 实现，实测全部命中）：**

| 文件 | R5 marker | 结果 |
|---|---|---|
| `format.js` | `jobStatusOptionsHtml` / `quick-continue--failed` / `btn-danger-solid` / `上次失败 · 重新运行` / `expired: "已过期"` | ✅ 全命中 |
| `dashboard-view.js` | `quick-row--failed` / `上次失败 · 重新运行` / `btn-danger-solid` / `is-loading` | ✅ 全命中 |
| `split-canvas.js` | `wb-action-bar` / `data-mobile-action-bar` / `workbenchPrimaryButtonHtml` | ✅ 全命中 |
| `kanban.js` | `boardCard(job, vocabulary.statuses)` | ✅ 命中 |
| `styles.css` | `.wb-action-bar` / `.board-status-select` / `--touch-min-safe` / `quick-row--failed` / `quick-continue--failed` | ✅ 全命中 |

---

## ② 逐项 PASS/FAIL 表

### 前置 — 新前端已生效

| 项 | 结果 | DOM/代码证据 |
|---|---|---|
| 页面已加载 R5 前端 | ✅ PASS | 5 个静态文件全部含 R5 marker（见上表）；`Cache-Control` 头为空 → 每次请求读盘，刷新即生效，无需重启 |

---

### P1-2 词表联动（桌面 1440×900，`#/jobs`）

| 检查点 | 结果 | DOM 证据（实测） |
|---|---|---|
| 卡片状态下拉 = 5 个 canonical option | ✅ PASS | 首卡 `select[data-board-status]`：`count=5`，values = `[draft, applied, interview, offer, withdrawn]`，选中态 = draft（岗位现状 `draft`） |
| 卡片下拉文案 = 设置词表 | ✅ PASS | labels = `[未投递, 已投递, 面试中, 已拿Offer, 放弃]`，与 `GET /api/settings → classification_vocabulary.statuses` **逐字一致**；源码路径 `format.js:92 jobStatusOptionsHtml()` ← `kanban.js:91 boardCard(job, vocabulary.statuses)` |
| 全部 11 张卡片同源 | ✅ PASS | 页面上 11 个 `select[data-board-status]`，`distinctOptionSets = 1`（所有卡片 option 组完全一致，无硬编码残留） |
| 筛选栏状态下拉同源 | ✅ PASS | `form[data-form="job-filter"] select[name="status"]`：5 options，文案 = 同词表逐字一致 |
| 编辑弹窗状态下拉同源 | ✅ PASS | `form[data-form="job-edit"] select[name="status"]`：5 options，文案 = 同词表逐字一致（`main.js:433 jobEditFormHtml ← vocabularyList("statuses")`） |
| 默认词表 5 态一致性 | ✅ PASS | 实例词表为默认值（未自定义），「默认 5 态同源一致性」成立：卡/筛/编三处 labels 均 == 词表 == `["未投递","已投递","面试中","已拿Offer","放弃"]`，且卡片 values 恒为 5 个 canonical key（FSM 完整） |
| 自定义词表路径（不可实机的部分） | ✅（代码级） | 实例词表=默认值，无法实机改词表（只读约束）；`tests/frontend/jobs-match.test.mjs` 新增 4 例验证重命名词表渲染、子集词表不至于收缩五态、缺词回退内建标签 —— **全部通过** |

**证据截图**：`ui-shots/r5-jobs-status-select.png`（桌面岗位库，首卡下拉展开 size=5）

> 注：卡片下拉 option **value 用 canonical key**（draft/…），筛选/编辑用词表原文（未投递/…）——文案同源成立；后端 `canonicalJobStatus()` 对两者均接受，属 P1-2 设计意图（保状态机完整 + 校验一致），非缺陷。

---

### P1-3 驾驶舱快速继续失败标识（`#/dashboard`）

| 检查点 | 结果 | DOM/代码证据（实测） |
|---|---|---|
| 快速继续卡存在 | ✅ PASS | `[data-quick-continue]` 渲染；`GET /api/dashboard → quick_continue = { job_id: 6f0378d4…, title: AI应用开发实习生-Cross Platform, alignment_status: "idle" }` |
| idle 态中性呈现（当前实机数据） | ✅ PASS | DOM：`cardClass="quick-row"`（无 failed 类）、badge `pill-warn`「待分析」、btn `btn btn-primary btn-sm`「继续」href `#/workspace/6f0378d4…`、无 aria-disabled —— 与 `dashboard-view.js` default 分支一致 |
| 文案统一（不再出现「继续对齐」） | ✅ PASS | 卡片 text = `AI应用开发实习生-Cross Platform 未识别公司 · 待分析 待分析 继续`，无「继续对齐」动词 |
| **failed/canceled/expired → 红警示** | ⚠️ 状态不可达（代码级 PASS） | 当前实例 **无任何 failed/canceled/expired 岗位**（见 ③ 数据限制）。代码级证据：`dashboard-view.js`（`f33b79a`）`qFailed → {quick-row--failed, badge-red「上次失败 · 重新运行」, btn-danger-solid「重新运行」}`；`styles.css` `.quick-row--failed { box-shadow: inset 4px 0 0 var(--error) }`；单测 `dashboard.test.mjs` 对 failed/canceled/expired 三态断言 `上次失败 · 重新运行` + `btn-danger-solid` —— **通过** |
| succeeded → 「已对齐/查看」 | ⚠️ 状态不可达（代码级 PASS） | quick_continue 选择器优先取非 succeeded 岗位（`dashboard.py:127`），实例存在 idle 岗位故 succeeded 卡不出现。代码级证据：badge-green「已对齐」+ btn-outline「查看」（`dashboard-view.js`/`format.js quickContinueHtml`），单测断言 link.text == 「查看」通过 |
| running/queued → 「分析中」禁用 | ⚠️ 状态不可达（代码级 PASS） | 无运行中任务。代码级证据：badge-blue「分析中」+ `is-loading` + `aria-disabled="true"` + 无 href；单测断言 href==null、aria-disabled==true 通过 |
| 失败态实机出现（若实例有失败任务） | — | 实例无失败任务，未强行触发真实 LLM 对齐（遵守只读约束） |

**证据截图**：`ui-shots/r5-dashboard-quick.png`（驾驶舱，quick card = idle 中性态）

---

### P1-4 移动端工作台可达性（390×844，`#/workspace/{job}`）

实测岗位：`bcb5d7bb9a91…`「后台开发」，`alignment_status=succeeded`（有真实对齐记录），`workbench_job_id=22f5f8e00311…`。

| 检查点 | 结果 | DOM 证据（实测） |
|---|---|---|
| ① 视口无横向滚动 | ✅ PASS | `document.documentElement.scrollWidth = 390 == clientWidth = 390`（`window.innerWidth=390`）。移动端 dashboard / jobs / workspace 三路由均无横向滚动 |
| ② 失败/可重试态底栏出现且 sticky | ⚠️ 状态不可达（代码级 PASS） | 无 failed/canceled/expired 岗位。代码级证据：`split-canvas.js:217 barActive = alignmentRunning \|\| ["failed","canceled","expired"].includes(alignment.status)` → 渲染 `[data-wb-action-bar]` 且 view 带 `data-mobile-action-bar="true"`；`styles.css` `@media (max-width:640px) .wb-action-bar { position: sticky; bottom: 0; z-index:30; … }`；单测 `workbench-guide.test.mjs`「failed alignment renders the mobile bottom action bar with the rerun button」通过（bar 内按钮文案「重新运行对齐」，running 态 bar 按钮 disabled） |
| ② 主按钮 min-height ≥ 40px | ✅ PASS | 移动工作台 `.workbench-view [data-action="run-alignment"]`：`getComputedStyle().minHeight = 40px`（`--touch-min-safe`），实际 rect 高 46px、`display:flex`、y=676（844 视口内可见） |
| ② 激活底栏时顶栏无同款重复按钮（单入口） | ✅ PASS（代码级 + 实机现状） | 实机 succeeded 态：`.wb-context-actions` 内 `[data-action="run-alignment"]` count=1（无重复）。代码级：`styles.css` `@media (max-width:640px) .workbench-view[data-mobile-action-bar="true"] .wb-context-actions [data-action="run-alignment"] { display:none }` —— 底栏激活时顶栏同款自动隐藏 |
| ③ 成功态底栏不常驻 | ✅ PASS（实机可达） | succeeded 工作台：`[data-wb-action-bar]` count = **0**，view 无 `data-mobile-action-bar` 属性 —— succeeded 不占底栏，与 8e58140 规格一致；顶栏主按钮仍可见（单入口保留） |
| ④ 工作台 tabs 与主操作 y 坐标（可达性现状） | ✅ PASS（现状报告） | `.wb-mobile-tabs`：`display:none`（≤900px 被 `styles.css:11959` 既有 `!important` 规则抑制，设计已切换为 `.wb-tabs` sticky）；`.wb-tabs`：`display:flex; position:sticky; top:0`，初始滚动位 y=5442、高 45px，tab 项（建议/岗位分析/面试记录/优化设置）y=5448；`.wb-context` y=393；主按钮 y=676。**完整 tabs 重组属 ticket #43，不在本次范围**，现状为可达：主按钮在首屏内（y=676 < 844），sticky tabs 滚动到对应区段可见 |
| 空态 `#/workspace` 走查 | ✅ PASS | 路由可达、无报错、无横向滚动；当前实现为「直达最近岗位工作台 + 数据岗位切换器」（`data-job-switcher` 存在，body 呈现「目标岗位 AI应用开发实习生-Cross Platform…」），可供 #43 空态设计参考（现状非独立选择器页） |
| 触控基线 `.board-status-select` ≥ 40px | ✅ PASS | 移动 `#/jobs`：`.board-status-select` computed `min-height: 40px`，rect 高 40px |
| 移动端错误 | ✅ PASS | 全流程 console/page/request 错误 = 0 |

**证据截图**：
- `ui-shots/r5-mb-workspace-bar.png` / `r5-mb-workspace-succeeded.png`（succeeded 工作台，无底栏、主按钮可见）
- `ui-shots/r5-mb-workspace-empty.png`（`#/workspace` 空态）
- `ui-shots/r5-mb-jobs.png`（移动岗位库，状态下拉 40px）

---

### ⑤ 桌面回归（1440×900）

| 路由 | 横向滚动 | console/page/request 错误 | 结论 |
|---|---|---|---|
| `#/dashboard` | 无（1440==1440） | 0 | ✅ 无回归 |
| `#/jobs` | 无 | 0 | ✅ 无回归 |
| `#/resumes` | 无 | 0 | ✅ 无回归 |
| `#/workspace/{bcb5d7bb…}` | 无 | 0 | ✅ 无回归 |

**证据截图**：`ui-shots/r5-desktop-dashboard.png`、`r5-desktop-jobs.png`、`r5-desktop-resumes.png`、`r5-desktop-workspace.png`

---

## ③ 数据限制说明（无法到达的状态如实标注）

1. **当前实例无 failed/canceled/expired 对齐任务**：`library_jobs` 11 行 = succeeded ×5 / idle ×6；`GET /api/dashboard` quick_continue=`idle`；SQLite 全库扫表（9 张业务表）未见 failed/canceled/expired 记录。早前走查的对齐失败岗位 `6f0378d4`（2026-08-25 走查时曾 failed）**现已回到 idle**，无失败历史残留表。
2. **约束**：未触发真实 LLM 对齐、未新建耗额度任务（验收指令明确禁止），故 P1-3 的 failed/succeeded/running 三种分型卡与 P1-4 失败态底栏 **实机不可达**。
3. 上述不可达项均以 **API 响应 + commit diff 源码 + 已通过的单测**（45/45，含 R5 新增用例）作为代码级验收证据，非凭空推断。
4. 词表为默认值（未自定义），P1-2 自定义词表路径以单测覆盖（重命名/子集/回退 4 例通过）。

---

## ④ 发现的新问题（若有）

| # | 严重度 | 描述 | 建议 |
|---|---|---|---|
| N1 | 低（观察项） | `#/workspace` 空态目前直达最近岗位工作台（带切换器），不是独立岗位选择器页 | 纳入 ticket #43 空态设计一并处理；本次仅报告现状 |
| N2 | 低（观察项） | 卡片下拉 value 用 canonical key（draft/…），筛选/编辑用词表原文（未投递/…），文案同源但值编码不同；后端 `canonicalJobStatus` 均接受 | 无需修复；如后续抽查可统一提交编码，收益低 |
| N3 | 信息 | 移动端 `.wb-mobile-tabs`（调优/结果分段钮）被 ≤900px 既有 `!important` 规则抑制，实机呈现为 `.wb-tabs` sticky（岗位分析/面试记录/优化设置/建议）| 属既有响应式设计（注释「宽屏 wb-tabs 恢复可见」），非 R5 引入；tabs 完整重组已入 #43 |

无阻塞/高风险问题。R5 未引入 JS 错误、横向滚动或桌面布局回归。

---

## ⑤ 结论

**R5 三项全部达成验收：**

| 项 | 结论 | 依据 |
|---|---|---|
| P1-2 词表联动 | ✅ 验收通过 | 卡片下拉（5 canonical option + 词表文案）与筛选栏、编辑弹窗三处同源实机逐字一致；11 卡全同源；单测 4 例补自定义/子集/回退路径 |
| P1-3 驾驶舱失败标识 | ✅ 验收通过（失败态实机不可达，代码级验证） | idle 中性态实机正确；failed/canceled/expired → 红左条 + badge-red「上次失败 · 重新运行」+ btn-danger-solid「重新运行」、succeeded →「已对齐/查看」、running →「分析中」禁用，均按 diff 实现且单测 4 组变体通过；未强触 LLM，无额度消耗 |
| P1-4 移动端可达性最小集 | ✅ 验收通过（失败态底栏实机不可达，代码级验证） | 390px 无横向滚动；succeeded 工作台无底栏常驻（实机）；主按钮 min-height 40px、状态下拉 40px（实机 computed）；失败/运行中态底栏 sticky + 顶栏单入口按 diff+单测验证 |
| 桌面回归 | ✅ 无回归 | 四路由 1440×900 无横向滚动、零 JS 错误 |

**验收方附注**：本报告为只读 DOM 走查，P1-3/P1-4 中失败态相关条目因实例数据中不存在失败任务而无法实机呈现，已按要求以「API 快照 + commit diff + 通过单测」作为证据；若后续实例出现失败任务（或验收环境允许构造），建议补跑一次实机红警示与底栏 sticky 视觉确认。